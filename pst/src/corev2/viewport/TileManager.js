import Tile from './Tile';
import Camera from './Camera';

// TODO: use this for tile managment
class TileManager {
  constructor() {
    this.currentContext = undefined;
    this.tileSize = 256;
  }
}

export default new TileManager();

{
  "name": "poestk",
  "description": "poestk",
  "version": "0.1.0",
  "engines": {
    "node": "10.6.0",
    "yarn": "1.7.0"
  },
  "scripts": {
    "start:windows": "SET NODE_ENV=production&& node server.js",
    "start": "NODE_ENV=production node server.js",
    "postinstall": "cd client && yarn install && yarn build:heroku"
  },
  "dependencies": {
    "compression": "^1.7.3",
    "express": "^4.16.3"
  }
}

import styled from 'styled-components'
import { colors } from '../constants'

const BuildInformation = styled.section`
  width: 300px;
  background-color: ${colors.gray};
  border-left: 2px solid ${colors.gray300};
  z-index: 2;
`

export default BuildInformation
